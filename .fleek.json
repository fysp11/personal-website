{
  "build": {
    "image": "node:18-alpine",
    "command": "npm install -g pnpm && pnpm install && npm run build && npm run export",
    "publicDir": "build",
    "baseDir": "frontend",
    "environment": {
      "SOME_ENV": "Build finished!"
    }
  }
}
