{
  "build": {
    "image": "node:18-alpine",
    "command": "npm install -g pnpm && pnpm install && npm run fleek:prepare && npm run build",
    "publicDir": "dist",
    "baseDir": "./",
    "environment": {}
  }
}
